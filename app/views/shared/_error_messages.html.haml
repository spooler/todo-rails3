- if target.errors.any?
  #error_explanation
    %h2
      = pluralize(target.errors.count, "error")
      = defined?(message) ? message : "prohibited this #{target_name} from being saved:"
    %ul
      - target.errors.full_messages.each do |msg|
        %li= msg